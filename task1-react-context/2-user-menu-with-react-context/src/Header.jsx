import React from 'react';
import UserMenu from './UserMenu';

const Header = () => {
  return (
    <header className="header">
      <UserMenu></UserMenu>
    </header>
  );
};

export default Header;
